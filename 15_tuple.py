# Tuple is a immutable(cannot be changed) collection of objects
# Tuple is faster than list. They can be used if a list doesn't require any change
department = ('Cardiology', 'Nephrology', 'Neurology', 'Urology')  # These departments in a hospital cannot be changed
print(department)